apiVersion: v1
kind: Service
metadata:
  name: minio-service
spec:
  clusterIP: None # Headless service for StatefulSet identity
  ports:
    - port: 9000
      name: api
    - port: 9001
      name: console
  selector:
    app: minio